import 'package:flutter/material.dart';

class ChequesScreen extends StatelessWidget {
  const ChequesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cheques Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
